# Environment Configuration
# Copy this file to .env and update the values

# Application
DEBUG=true
SECRET_KEY=change-me-in-production
HOST=0.0.0.0
PORT=8000

# OpenRouteService — required for POST /api/trip/plan.
# Without it, the endpoint returns 503. Get a free key at https://openrouteservice.org/dev/
ORS_API_KEY=
